/// UIConstants provides shared paddings, margins, and spacing for consistent UI.
library;
import 'package:flutter/material.dart';

class UIConstants {
  static const EdgeInsets pagePadding = EdgeInsets.symmetric(horizontal: 24, vertical: 16);
  static const EdgeInsets sectionPadding = EdgeInsets.symmetric(vertical: 12);
  static const EdgeInsets cardPadding = EdgeInsets.all(16);
  static const double elementSpacing = 12.0;
  static const double sectionSpacing = 20.0;
  static const double chipSpacing = 8.0;
  static const double fieldSpacing = 16.0;
} 